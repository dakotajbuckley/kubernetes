apiVersion: v1
kind: Service
metadata:
  name: LoadBalancer
  labels:
    app: front-end
spec:
  type: LoadBalancer
  ports:
    - port: 80
      tagetPort: 80
      nodePort: 31000
  selector:
    app: front-end
